function s_ogbox = initialize_ogbox(ogbox_com)
% ogbox_com: OGBox Ardino serial port name, specified as a character vector or string scalar. Use serialportlist to get a list of connected ports. Example: "COM2"
% s_ogbox: returned serial port object

fprintf('Initializing OGBox arduino\n');
s_ogbox = serial(config.ogbox_com);
s_ogbox.BaudRate=9600;
fopen(s_ogbox);
pause(2)

end
